next steps:

	forest_004, die and you get transported to the previous screen, bad 
	positioning???, does not grab ladder??? check that.

	group_draw center, take into account visibility in libdansdl2 

	tile draw animated thing, make it equivalent to the other. 
		I don't know what this means.

	the console does not seem to stop capturing text after it exits

	test map with EVERYTHING IN THE GAME and a shell script to start it.

	fix crash at exit
		seems to be something about music unloading. I just commented it out.

	sound:
		what about a very basic sound like 
			play_sound(int)
			play_looped(int) : int
			play_exclusive(int)

	make the first example game
		- trap:
			fire falling in drops from the ceiling, with gravity??

		make a paper map for it.

		use the breaking platforms!!

		"something" to reskin the character?

		pause should run for at least some ms... That would be easy, just 
		reset a counter on awakening and set the "can_leave_state" flag
		accordingly.

		draw status (status screen??)
			shows current items
			shows current time (in negative steps XD)
			shows current lives (if need be).

	show data on savegame loading!
		for this we need to store the items picked up

	add lives
		and game over screen
		and lives remaining stuff
		and gain a life each N bonuses

	add timer

	confirm on savegame deletion

	general map:

				<GREEN>
			/         \
		castle        cave
			|          |
	[SHORTCUT]        [SHORTCUT]
			|          |
		mountain      ???
			|          |
		<RED>          <BLUE>
			|          |
		forest        river
			\	       /
				START

		- 2 keys on each regular area (not <> or []) for a total of 12
		- 4 keys to enter each <>

		-castle area
			- front and back, moving through windows and doors
			- secret on ceiling climbing lamp hole.

	engine:
		I guess slopes
		Or moving blocks, which were originally there too.
		tile graphics
			maybe import the whole composer thingy.
		the tile drawer cannot do stuff like scale or more sophisticated
			sprite scaling.

	docs:
		Complete the README
